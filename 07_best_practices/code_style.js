// code_style.js
// Consistent code style improves readability

function add(a, b) {
  return a + b;
}

const result = add(2, 3);
console.log(result);
// Best practice: Use consistent indentation, spacing, and semicolons. Use linters and formatters like ESLint and Prettier.
