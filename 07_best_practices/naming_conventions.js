// naming_conventions.js
// Use clear, descriptive names for variables, functions, and classes

const userName = "Alice"; // camelCase for variables and functions
function getUserName() {
  return userName;
}
class UserProfile {
  // PascalCase for classes
  constructor(name) {
    this.name = name;
  }
}
// Best practice: Avoid abbreviations and single-letter names except for counters.
