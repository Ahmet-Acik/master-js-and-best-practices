// operators.js
// Arithmetic, assignment, comparison, logical, and more

let a = 5;
let b = 2;
console.log(a + b, a - b, a * b, a / b, a % b); // Arithmetic
console.log(a == b, a === b, a != b, a !== b); // Comparison
console.log(a > b && b < 10); // Logical
// Best practice: Use '===' and '!==' for strict equality checks.
